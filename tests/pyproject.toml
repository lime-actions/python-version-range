[build-system]
requires = [
    "setuptools>=64.0",
]
build-backend = "setuptools.build_meta"

[project]
name = "test"
authors = [
    { name = "Limespy" },
]

requires-python = ">=3.10"
readme = "README.md"
version = "0.7.0"
